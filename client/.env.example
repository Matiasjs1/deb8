# For LOCAL dev only (Vite dev server on 5173 talking to the API on 4000).
# Do NOT set this in production: on the single-host Render service the client
# is served by the same Express server, so an (unset) empty VITE_API_BASE is
# correct and the frontend calls the same origin.
VITE_API_BASE=http://localhost:4000
